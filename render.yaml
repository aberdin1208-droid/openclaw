services:
    - type: web
    name: openclaw
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
            - key: OPENCLAW_GATEWAY_PORT
        value: 10000
            - key: OPENCLAW_GATEWAY_TOKEN
        sync: false
            - key: GROQ_API_KEY
        sync: false
